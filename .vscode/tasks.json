{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "Run Mega Monitor Backend with Logging",
			"command": "uvicorn main:app --reload --log-level debug --log-config backend_debug.log",
			"isBackground": true
		}
	]
}